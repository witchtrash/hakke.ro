import React from 'react';
import styled from '@emotion/styled';
import { Flex } from './Container';

const StyledFlex = styled(Flex)`
  flex: 1 1 auto;
`;

export const FlexSpacer = () => <StyledFlex />;
