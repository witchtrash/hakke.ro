import React from 'react';
import { Logo } from '~/components/Logo';
import { Layout } from '~/components/Layout';

const Home = () => (
  <Layout>
    Hello world!
    <Logo />
  </Layout>
);

export default Home;
